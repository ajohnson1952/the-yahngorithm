# Manual trigger for the "weather" group (weather forecast + injuries).
# Scheduling is handled by tick.yml + cron-job.org.
name: Manual — weather + injuries

on:
  workflow_dispatch:

jobs:
  go:
    uses: ./.github/workflows/_run.yml
    secrets: inherit
    with:
      commands: npm run tick -- --only weather
